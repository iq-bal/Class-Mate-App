import 'package:flutter/material.dart';

void main() {
  runApp(MaterialApp(
    home: Scaffold(
      appBar: AppBar(title: Text('Fixed File')),
      body: Center(child: Text('This is a fixed file')),
    ),
  ));
}
